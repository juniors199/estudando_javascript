const nome = prompt("Qual seu nome: ");

if(nome == "Marcio"){
    console.log(`Saudações ${nome}, o seu usuario esta correto`);
} else {
    console.log(`Usuario incorreto, o seu é ${nome}`);
}