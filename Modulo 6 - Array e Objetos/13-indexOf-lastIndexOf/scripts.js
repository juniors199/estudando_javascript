let numeros = [0,1,2,1,0];

// indexOf = varre a lista e econtra o indice do primeiro elemento encontrado.
// lastIndexOf = varre a lista e encontra o indice do ultimo elemento encontrado.

console.log(numeros.indexOf(1)); // 1
console.log(numeros.lastIndexOf(1)); // 3

let nums = [5,6,2,4,9,6,2];


console.log(nums.indexOf(2)); // 2
console.log(nums.lastIndexOf(2)); // 6

